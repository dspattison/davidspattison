<%= form_for(@tte_game) do |f| %>
  <% if @tte_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tte_game.errors.count, "error") %> prohibited this tte_game from being saved:</h2>

      <ul>
      <% @tte_game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Your Opponent's email address:" %><br />
    <%= f.text_field :player_a_email %>
  </div>
  <div class="field">
    <%= f.label "Your email address:" %><br />
    <%= f.text_field :player_b_email %>
  </div>
  
  <div class="field">
  	<%= f.label "Your first move as X:" %><br />
  	<%= render 'board_form' %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
