<%= form_for(@c4_game) do |f| %>
  <% if @c4_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@c4_game.errors.count, "error") %> prohibited this c4_game from being saved:</h2>

      <ul>
      <% @c4_game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Opponent's email" %><br />
    <%= f.text_field :player_a_email %>
  </div>
  <div class="field">
    <%= f.label "Your email" %><br />
    <%= f.text_field :player_b_email %>
  </div>
  <div class="field">
    <p>Please select a cell to start the game</p>
    <%= render :partial=> 'board_form'%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
